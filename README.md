# Local Database Project

This project contains the Data Definition Language (DDL) and other SQL scripts for a local database.

## Files

*   `created_at&updated_at_in_all_tables.sql`: This script likely adds `created_at` and `updated_at` columns to all tables in the database to track record creation and updates.
*   `f_save_lease_history_data.sql`: This script probably contains a function to save lease history data.
*   `parking_space_type_enum.sql`: This script likely defines a custom enumeration type for parking space types.
*   `tenant_type_enum.sql`: This script probably defines a custom enumeration type for tenant types.
*   `trg_save_lease_data_record.sql`: This script likely contains a trigger to save a record of lease data changes.
*   `DDL.sql`: This script likely contains the main DDL for creating the database schema.
